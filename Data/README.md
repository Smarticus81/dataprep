# Data folder

Drop the raw source files you want to standardize into this folder.

Supported inputs include CSV, XLSX, JSON, DOCX, and PDF files. You can place all files for one package directly in this folder, then run:

```bash
python -m input_standardizer.cli
```

