# Output folder

The standardizer writes the canonical package here when you run:

```bash
python -m input_standardizer.cli
```

Generated files include `preparer_manifest.json`, `preparer_issues.json`, `source_traceability.json`, and any canonical CSV or JSON files produced from the source data.

